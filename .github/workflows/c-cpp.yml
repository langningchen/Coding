name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: gcc "P8319 『JROI-4』分数.cpp" -o test.exe -std=c++11 -lstdc++
